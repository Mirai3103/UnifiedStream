package com.laffy.unifiedstream.ui.theme

import androidx.compose.ui.graphics.Color

/** Page background. */
val Surface0 = Color(0xFF0B0D12)

/** Raised card surface. */
val Surface1 = Color(0xFF141821)

/** Inset rows inside a card. */
val Surface2 = Color(0xFF1B2130)

val TextPrimary = Color(0xFFE8EAF0)
val TextMuted = Color(0xFF8D94A5)
val OutlineSubtle = Color(0xFF2A3243)

val Accent = Color(0xFF60A5FA)

/** Link-quality colours. Distinct in hue and lightness, so they survive a glance. */
val Good = Color(0xFF34D399)
val Degraded = Color(0xFFFBBF24)
val Poor = Color(0xFFF87171)
